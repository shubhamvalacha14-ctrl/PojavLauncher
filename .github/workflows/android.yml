name: Android CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Debug APK with Force Flags
        run: |
          chmod +x ./gradlew
          ./gradlew :app_pojavlauncher:assembleDebug -Pandroid.defaults.buildfeatures.buildconfig=true --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-Custom-PojavLauncher
          path: app_pojavlauncher/build/outputs/apk/debug/app_pojavlauncher-debug.apk
          
